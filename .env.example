# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                     BrightStart Environment Configuration                      ║
# ║                                                                                ║
# ║  Copy this file to .env and adjust the values according to your needs.        ║
# ║  Lines starting with # are comments and will be ignored.                       ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                           FLASK CONFIGURATION                                 │
# └──────────────────────────────────────────────────────────────────────────────┘

# FLASK_ENV: Application environment mode
# Options: development | production | testing
# - development: Enables debug mode, auto-reload, detailed error pages
# - production: Optimized for deployment, no debug info exposed
# - testing: For running automated tests
# Effect: Controls debug mode, error handling, and logging verbosity
FLASK_ENV=development

# FLASK_DEBUG: Enable/disable debug mode explicitly
# Options: 1 (enabled) | 0 (disabled)
# Effect: When enabled, shows detailed error pages and enables auto-reload
# WARNING: Never enable in production - exposes sensitive information!
FLASK_DEBUG=1

# SECRET_KEY: Cryptographic key for session security
# Format: Any random string (recommended: 32+ characters)
# Effect: Used for signing cookies, CSRF tokens, and session data
# WARNING: Change this in production! Use a strong random key.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-this-in-production

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          ADMIN ACCOUNT SETTINGS                               │
# └──────────────────────────────────────────────────────────────────────────────┘

# ADMIN_USERNAME: Default admin account username
# Effect: Created automatically on first run if no admin exists
# Tip: Change after first login via admin panel
ADMIN_USERNAME=admin

# ADMIN_PASSWORD: Default admin account password
# Effect: Initial password for admin account
# WARNING: Change immediately after first login!
ADMIN_PASSWORD=informatika

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          DATABASE CONFIGURATION                               │
# └──────────────────────────────────────────────────────────────────────────────┘

# DATABASE_URL: Database connection string
# Format: dialect://username:password@host:port/database
# Examples:
# SQLite (local):     sqlite:///instance/brightstart.db
#   SQLite (absolute):  sqlite:///D:/path/to/brightstart.db
#   PostgreSQL:         postgresql://user:pass@localhost:5432/brightstart
#   MySQL:              mysql://user:pass@localhost:3306/brightstart
# Effect: Determines where all app data is stored
# Default: Uses absolute path to instance/brightstart.db if not set
# NOTE: Leave commented to use default absolute path (recommended)
DATABASE_URL=sqlite:///instance/brightstart.db

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                           MODEL CONFIGURATION                                 │
# └──────────────────────────────────────────────────────────────────────────────┘

# MODEL_DOWNLOAD_URL: URL to download the ML model if not present locally
# Format: Direct download URL to .keras or .h5 file
# Effect: If model file is missing, app will attempt to download from this URL
# Tip: Use GitHub Releases or cloud storage for hosting
# Leave empty to skip auto-download (model must be placed manually)
MODEL_DOWNLOAD_URL=https://github.com/your-repo/releases/download/v1.0/InceptionResNetV2.keras

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                           SECURITY CONFIGURATION                              │
# └──────────────────────────────────────────────────────────────────────────────┘

# CORS_ORIGINS: Allowed origins for Cross-Origin Resource Sharing
# Format: Comma-separated URLs or * for all origins
# Examples:
#   Allow all:          *
#   Single origin:      https://example.com
#   Multiple origins:   https://app1.com,https://app2.com
# Effect: Controls which domains can make API requests to your app
# WARNING: Using * in production may pose security risks
CORS_ORIGINS=*

# CSRF_TRUSTED_ORIGINS: Trusted origins for CSRF protection
# Format: Comma-separated URLs (must include scheme)
# Examples:
#   https://example.com,https://app.example.com
# Effect: Requests from these origins skip CSRF origin check
# Note: Leave empty for local development
CSRF_TRUSTED_ORIGINS=

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                           UPLOAD CONFIGURATION                                │
# └──────────────────────────────────────────────────────────────────────────────┘

# MAX_CONTENT_LENGTH: Maximum upload file size in MB
# Format: Integer (megabytes)
# Examples:
#   5:   5 MB
#   10:  10 MB
#   50:  50 MB
# Effect: Files larger than this will be rejected with 413 error
# Note: Also affects request body size for all endpoints
MAX_CONTENT_LENGTH=10

# UPLOAD_FOLDER: Directory for storing uploaded images
# Format: Relative or absolute path
# Examples:
#   Relative:  uploads
#   Absolute:  D:/data/brightstart/uploads
# Effect: All prediction images are stored here
# Note: Directory will be created automatically if it doesn't exist
# WARNING: Ensure adequate disk space and proper permissions
UPLOAD_FOLDER=uploads

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                           LOGGING CONFIGURATION                               │
# └──────────────────────────────────────────────────────────────────────────────┘

# LOG_LEVEL: Minimum log level to record
# Options: DEBUG | INFO | WARNING | ERROR | CRITICAL
# - DEBUG:    All messages (most verbose, for development)
# - INFO:     General operational messages
# - WARNING:  Potential issues that don't stop execution
# - ERROR:    Errors that prevent specific operations
# - CRITICAL: Severe errors that may crash the application
# Effect: Messages below this level are ignored
# Note: Logs are always saved to logs/brightstart.log
LOG_LEVEL=DEBUG

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                           SESSION CONFIGURATION                               │
# └──────────────────────────────────────────────────────────────────────────────┘

# PERMANENT_SESSION_LIFETIME: How long users stay logged in (in days)
# Format: Integer (days)
# Examples:
#   1:   1 day
#   7:   7 days (1 week)
#   30:  30 days (1 month)
# Effect: Users are automatically logged out after this period of inactivity
# Note: Applies when "Remember Me" is checked during login
PERMANENT_SESSION_LIFETIME=30
